<UserControl x:Class="ER_Diagram_Modeler.Views.Canvas.TableItem.TableViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ER_Diagram_Modeler.Views.Canvas.TableItem"
             xmlns:viewModels="clr-namespace:ER_Diagram_Modeler.ViewModels"
             mc:Ignorable="d" 
			 d:DataContext="{d:DesignInstance viewModels:TableViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"></RowDefinition>
			<RowDefinition Height="*"></RowDefinition>
		</Grid.RowDefinitions>
		<TextBlock x:Name="TableTitle" 
				   Grid.Row="0" 
				   Text="{Binding Model.Title}" 
				   Background="{StaticResource PrimaryColorBrush}" 
				   VerticalAlignment="Center" 
				   Padding="5"
				   FontWeight="Bold"
				   Foreground="White"
				   IsHitTestVisible="False"></TextBlock>
		<Border Grid.Row="1" Background="{StaticResource PrimaryColorBrush}" BorderThickness="3" BorderBrush="{StaticResource PrimaryColorBrush}">
			<DataGrid 
			ItemsSource="{Binding Model.Attributes}"></DataGrid>
		</Border>
	</Grid>
</UserControl>
